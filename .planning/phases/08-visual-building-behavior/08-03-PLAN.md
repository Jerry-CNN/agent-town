---
phase: 08-visual-building-behavior
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [BLD-02, BLD-03, VIS-01, VIS-02]
must_haves:
  truths:
    - "Building outlines visible on the map at default zoom"
    - "Agent labels readable at default zoom — white text on dark pills"
    - "Sector labels readable at 28px with white halo"
    - "Simulation runs without errors with operating hours enforcement active"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 8 visual and behavioral changes work correctly together: wall outlines render, agent text is readable, operating hours are enforced, and ejection works at hour boundaries.

Purpose: Phase 8 modifies both frontend rendering and backend simulation logic in parallel. This checkpoint confirms the full system works end-to-end after both plans complete.

Output: Human-verified confirmation that Phase 8 visual and behavioral goals are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-visual-building-behavior/08-CONTEXT.md
@.planning/phases/08-visual-building-behavior/08-01-SUMMARY.md
@.planning/phases/08-visual-building-behavior/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start dev servers</name>
  <files></files>
  <read_first>
    - .planning/phases/08-visual-building-behavior/08-01-SUMMARY.md
    - .planning/phases/08-visual-building-behavior/08-02-SUMMARY.md
  </read_first>
  <action>
1. Run the full backend test suite to confirm no regressions:
   ```bash
   cd /Users/sainobekou/projects/agent-town && python -m pytest tests/ -x -v
   ```

2. Run the frontend TypeScript check:
   ```bash
   cd /Users/sainobekou/projects/agent-town/frontend && npx tsc --noEmit
   ```

3. Start both backend and frontend dev servers so the user can inspect visually:
   - Backend: `cd /Users/sainobekou/projects/agent-town && uvicorn backend.main:app --reload`
   - Frontend: `cd /Users/sainobekou/projects/agent-town/frontend && npm run dev`

Report the test results and server URLs to the user.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town && python -m pytest tests/ -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All backend tests pass (including test_building_hours.py)
    - Frontend TypeScript compiles without errors
    - Both dev servers start without crashes
  </acceptance_criteria>
  <done>Full test suite green, both servers running, ready for visual inspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of visual and behavioral changes</name>
  <files></files>
  <action>
Present the following verification checklist to the user. Phase 8 built:
- Building wall outlines (3px dark stroke around each sector zone)
- Agent label readability (20px name / 16px activity, white text on dark semi-transparent pills)
- Sector labels (28px with white stroke halo)
- Simulation time tracking (starts 7am, advances 10 sim-minutes per tick)
- Operating hours enforcement (closed buildings excluded from agent decisions)
- Agent ejection when building closes (agent leaves immediately, re-decides next tick)

Verification steps for the user:
1. Open http://localhost:5173 in browser
2. Configure LLM provider if needed (first-visit setup)
3. Start the simulation (unpause)

Visual checks (VIS-01, VIS-02, BLD-02):
4. Verify each sector zone (park, cafe, shop, office, stock-exchange, wedding-hall, homes) has a visible dark outline border
5. Verify agent name labels are large, white text on dark semi-transparent pill background
6. Verify activity text above agents is readable white-on-dark, truncated with ellipsis if long
7. Verify sector labels (e.g., "Cafe", "Park") are large and readable

Behavioral checks (BLD-03):
8. Let the simulation run for 2-3 minutes — agents should make decisions normally
9. Watch the activity feed for any agent with "leaving (building closed)" — this confirms ejection works when a building's hours expire
10. Confirm agents do not navigate to locations that should be closed (e.g., stock exchange should close around sim hour 17)

Wait for user response: "approved" or description of issues.
  </action>
  <verify>
    <automated>echo "Human checkpoint — awaiting user approval"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms building outlines are visible on all sector zones
    - User confirms agent text is readable (white on dark pill)
    - User confirms simulation runs without errors
    - User approves with "approved" signal
  </acceptance_criteria>
  <done>User has approved all visual and behavioral changes for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this verification plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | - | - | - | Verification-only plan; no new code |
</threat_model>

<verification>
1. All backend tests pass
2. Frontend compiles cleanly
3. Human confirms visual improvements in browser
4. Human confirms operating hours behavior in running simulation
</verification>

<success_criteria>
- Human approves visual rendering of wall outlines and agent labels
- Human confirms simulation runs without errors with operating hours active
- No test regressions across the full suite
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-building-behavior/08-03-SUMMARY.md`
</output>
