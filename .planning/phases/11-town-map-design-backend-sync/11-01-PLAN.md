---
phase: 11-town-map-design-backend-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync_map.py
  - scripts/validate_map.py
  - tests/test_sync_map.py
  - tests/conftest.py
  - docs/tiled-authoring-guide.md
autonomous: true
requirements:
  - TOWN-01
  - TOWN-02
  - TOWN-03

must_haves:
  truths:
    - "sync_map.py converts a Tiled TMJ export into valid town.json + buildings.json + frontend town.json"
    - "sync_map.py preserves the exact backend schema (size=[height,width], sparse tiles, Building dataclass fields)"
    - "Collision rectangles from Tiled are correctly converted to collision:true tile entries"
    - "Sector and arena addresses from Tiled object layers are correctly assigned to tiles"
    - "Building metadata (display_name, opens, closes, purpose) is extracted from Tiled custom properties"
    - "A written guide exists that tells the user exactly how to set up the Tiled project"
  artifacts:
    - path: "scripts/sync_map.py"
      provides: "TMJ-to-JSON sync script"
      exports: ["extract_map", "main"]
    - path: "tests/test_sync_map.py"
      provides: "Unit tests for sync_map.py against minimal TMJ fixture"
      min_lines: 80
    - path: "docs/tiled-authoring-guide.md"
      provides: "Step-by-step Tiled project setup and export instructions"
      min_lines: 50
  key_links:
    - from: "scripts/sync_map.py"
      to: "backend/data/map/town.json"
      via: "JSON file write"
      pattern: "json\\.dump"
    - from: "scripts/sync_map.py"
      to: "backend/data/map/buildings.json"
      via: "JSON file write"
      pattern: "json\\.dump"
    - from: "tests/test_sync_map.py"
      to: "scripts/sync_map.py"
      via: "import"
      pattern: "from scripts\\.sync_map import"
---

<objective>
Build the sync_map.py script that converts a Tiled TMJ export into backend/frontend JSON files, write comprehensive tests against a minimal TMJ fixture, and produce a Tiled authoring guide for the user.

Purpose: This is the tooling foundation. The sync script is the bridge between the user's Tiled map authoring (Plan 02) and the backend/frontend integration (Plan 03). Tests validate the script works before the real map exists.

Output: scripts/sync_map.py, tests/test_sync_map.py, tests/conftest.py update, docs/tiled-authoring-guide.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-town-map-design-backend-sync/11-CONTEXT.md
@.planning/phases/11-town-map-design-backend-sync/11-RESEARCH.md

@backend/simulation/world.py
@backend/data/map/town.json
@backend/data/map/buildings.json
@backend/data/agents/alice.json
@tests/test_world.py
@scripts/copy_assets.py

<interfaces>
<!-- Key schemas the sync script must produce. Verified from direct codebase reads. -->

From backend/simulation/world.py (Maze.__init__):
```python
# size is [height, width] — CRITICAL: height FIRST
self.height, self.width = config["size"]
# Tile sparse list format:
# {"coord": [x, y], "address": ["sector", "arena"], "collision": true/false}
# address is optional (omit for anonymous walkable tiles)
# collision is optional (omit when False)
```

From backend/data/map/buildings.json (Building dataclass fields):
```python
# Each building dict must have exactly these keys:
# {"name": str, "sector": str, "opens": int, "closes": int, "purpose": str}
```

From backend/data/agents/alice.json (agent coord + spatial tree):
```json
{
  "coord": [12, 40],
  "spatial": {
    "address": {
      "living_area": ["agent-town", "home-alice", "bedroom"],
      "workplace": ["agent-town", "cafe", "counter"]
    },
    "tree": {
      "agent-town": {
        "home-alice": {"bedroom": [], "living-room": [], "kitchen": []},
        "cafe": {"seating": [], "counter": [], "kitchen": []}
      }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_sync_map.py with minimal TMJ fixture and sync_map.py</name>
  <files>tests/test_sync_map.py, tests/conftest.py, scripts/sync_map.py</files>
  <read_first>
    - backend/simulation/world.py (lines 175-212 for Maze.__init__ schema expectations)
    - backend/data/map/town.json (first 30 lines for schema structure)
    - backend/data/map/buildings.json (full file for Building dict format)
    - backend/data/agents/alice.json (full file for coord + spatial tree format)
    - tests/test_world.py (first 55 lines for test fixture patterns)
    - scripts/copy_assets.py (for existing script patterns)
  </read_first>
  <behavior>
    - Test: extract_map() on minimal 10x10 TMJ produces town_json with "size": [10, 10] (height, width)
    - Test: extract_map() marks collision rectangle tiles as {"collision": true} in output
    - Test: extract_map() assigns sector address ["sector-name"] to tiles within Sectors rectangle
    - Test: extract_map() assigns arena address ["sector-name", "arena-name"] to tiles within Arenas rectangle (name format "sector:arena")
    - Test: extract_map() collision tiles are NOT overwritten by arena addresses (collision priority)
    - Test: extract_buildings() extracts display_name, opens, closes, purpose from Tiled custom properties
    - Test: extract_buildings() uses defaults (opens=0, closes=24, purpose="general") when properties missing
    - Test: extract_spawn_points() converts pixel coords to tile coords (divide by 32)
    - Test: sync_map.py raises ValueError when required layer ("Sectors", "Arenas", "Collision", "Spawn Points") is missing
    - Test: sync_map.py raises ValueError when metadata layer has wrong type (tilelayer instead of objectgroup)
    - Test: arena name without colon separator raises ValueError
    - Test: float coordinates in Tiled objects are handled correctly (int conversion)
  </behavior>
  <action>
    1. Add a `minimal_tmj` fixture to tests/conftest.py: a 10x10 TMJ dict with all 14 layers (10 visual tilelayers with empty data arrays, 4 objectgroup metadata layers). Include:
       - One sector rectangle named "test-cafe" at pixels (64, 64) size (128, 96) = tiles (2,2)-(5,4) with custom properties: display_name="Test Cafe", opens=7, closes=22, purpose="food"
       - One arena rectangle named "test-cafe:seating" at pixels (64, 64) size (64, 64) = tiles (2,2)-(3,3)
       - One collision rectangle at pixels (0, 0) size (320, 32) = tiles (0,0)-(9,0) marking top border
       - One spawn point named "alice" at pixel (96, 96) = tile (3, 3)
       - Use tilewidth: 32, tileheight: 32, width: 10, height: 10

    2. Write tests/test_sync_map.py with all behaviors listed above. Use the conftest `minimal_tmj` fixture. Tests import `from scripts.sync_map import extract_map, extract_buildings, extract_spawn_points`.

    3. Create scripts/sync_map.py implementing:
       - `TILE_SIZE = 32` constant
       - `_obj_to_tile_bounds(obj) -> tuple[int,int,int,int]`: convert pixel-space object to tile bounds. Always `int()` before `//` to handle floats (Pitfall 2).
       - `_get_property(obj, name, default=None)`: get named custom property from Tiled object's properties array.
       - `_get_layers_by_name(tmj) -> dict`: build {name: layer} mapping from tmj["layers"]. Validate required metadata layers exist and have type "objectgroup" (Pitfall 4).
       - `extract_map(tmj: dict) -> dict`: produces town_json dict. Processing order: (1) mark collision rectangles as {"coord": [x,y], "collision": true}, (2) assign sector addresses (skip collision tiles), (3) assign arena addresses (split name on ":" — validate exactly one colon per Pitfall 5, skip collision tiles). Size field MUST be [height, width] per Pitfall 3. World name is "agent-town". tile_address_keys is ["world", "sector", "arena"].
       - `extract_buildings(sector_objects: list) -> list[dict]`: produce buildings.json array. Each entry: {"name": display_name or obj.name, "sector": obj.name, "opens": int, "closes": int, "purpose": str}. Use defaults for missing properties.
       - `extract_spawn_points(spawn_objects: list) -> dict[str, list[int]]`: produce {agent_name: [tile_x, tile_y]}.
       - `main(tmj_path, backend_dir, frontend_dir)`: CLI entry point. Reads TMJ, calls extract functions, writes three JSON files: backend/data/map/town.json, backend/data/map/buildings.json, frontend/src/data/town.json (same content as backend town.json for backward compatibility). Also writes spawn points to backend/data/map/spawn_points.json. Also updates each agent JSON file in backend/data/agents/ if agent name matches a spawn point (updates "coord" field in-place, per Research open question 3).
       - CLI via `if __name__ == "__main__"`: accept `--tmj PATH` argument (defaults to `frontend/public/assets/tilemap/town.tmj`). Print summary of sectors found, buildings generated, spawn points extracted, and any warnings.

    Per D-10: script lives in scripts/ directory following existing copy_assets.py pattern.
    Per D-11: buildings.json generated from Tiled custom properties, not hand-maintained.
    Per Pitfall 3: size field is [height, width].
    Per Pitfall 8: collision processed first, addresses skip collision tiles.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town && python -m pytest tests/test_sync_map.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_sync_map.py` returns at least 10
    - `grep "def extract_map" scripts/sync_map.py` returns a match
    - `grep "def extract_buildings" scripts/sync_map.py` returns a match
    - `grep "def extract_spawn_points" scripts/sync_map.py` returns a match
    - `grep 'size.*height.*width' scripts/sync_map.py` or `grep "height, width" scripts/sync_map.py` confirms correct size field ordering
    - `grep "objectgroup" scripts/sync_map.py` confirms metadata layer type validation
    - `grep "collision" scripts/sync_map.py` confirms collision-first processing
    - `python -m pytest tests/test_sync_map.py -x -q` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    sync_map.py correctly parses a minimal TMJ fixture and produces valid town.json (with correct size=[height,width], collision tiles, sector/arena addresses), buildings.json (with all Building dataclass fields), and spawn points. All 10+ unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Tiled authoring guide for the user</name>
  <files>docs/tiled-authoring-guide.md</files>
  <read_first>
    - .planning/phases/11-town-map-design-backend-sync/11-RESEARCH.md (lines 389-400 for authoring guide deliverable spec)
    - .planning/phases/11-town-map-design-backend-sync/11-CONTEXT.md (decisions D-06 through D-09, D-13)
    - frontend/public/assets/tilemap/ (list directory to confirm tileset PNG names)
    - backend/data/agents/alice.json (for agent name list and spatial tree example)
  </read_first>
  <action>
    Create docs/tiled-authoring-guide.md with these exact sections:

    **1. Project Setup**
    - New Map: 140 tiles wide x 100 tiles tall, 32x32px tiles, Orthogonal orientation, CSV tile layer format.
    - Save as: `frontend/public/assets/tilemap/town.tmx` (working file).

    **2. Add Tilesets**
    - List all 16 CuteRPG PNG filenames from `frontend/public/assets/tilemap/` (get the actual file list). Each tileset added with "Embed in map" UNchecked, relative path like `./CuteRPG_Field_B.png`. Tile size 32x32 for all.

    **3. Create Layers (EXACT ORDER)**
    Visual tile layers (type: Tile Layer):
    1. Bottom Ground
    2. Exterior Ground
    3. Exterior Decoration L1
    4. Exterior Decoration L2
    5. Interior Ground
    6. Wall
    7. Interior Furniture L1
    8. Interior Furniture L2
    9. Foreground L1
    10. Foreground L2

    Metadata object layers (type: Object Layer, set visible: false):
    11. Sectors
    12. Arenas
    13. Collision
    14. Spawn Points

    Include a note: layer names must match EXACTLY (case-sensitive) for pixi-tiledmap rendering in Phase 12.

    **4. Sector Objects (in Sectors layer)**
    - Draw a rectangle over each building/area zone
    - Set rectangle Name = sector key (exact values): cafe, stock-exchange, wedding-hall, park, office, shop, home-alice, home-bob, home-carla, home-david, home-emma, home-frank, home-grace, home-henry
    - Add custom properties to each sector rectangle:
      - `display_name` (string): human-readable name, e.g. "Town Cafe"
      - `opens` (int): opening hour (0-24), e.g. 7
      - `closes` (int): closing hour (0-24), e.g. 22
      - `purpose` (string): one of "food", "finance", "social", "leisure", "work", "retail", "residential"
    - Include a reference table of all 14 sectors with their display_name, opens, closes, purpose values (copy from current buildings.json but use 8 homes for alice-henry per D-03, optionally add isabel/james if space allows).

    **5. Arena Objects (in Arenas layer)**
    - Draw rectangles for sub-zones within each sector
    - Name format: `sector:arena` (colon separator REQUIRED), e.g. `cafe:seating`, `cafe:counter`, `cafe:kitchen`
    - List all required arenas from agent spatial trees (read all 8 agent JSON files to compile the complete list of sector:arena pairs). For example: cafe:seating, cafe:counter, cafe:kitchen, stock-exchange:trading-floor, etc.

    **6. Collision Objects (in Collision layer)**
    - Draw rectangles over all non-walkable areas (building walls, water, dense trees, map borders)
    - IMPORTANT: Painting walls on the Wall visual layer does NOT block pathfinding. Every wall MUST also have a matching Collision rectangle.
    - Snap to grid (32px) for clean tile alignment.
    - Include border rectangles: top row (0,0 to 4480,32), bottom row (0,3168 to 4480,32), left column (0,0 to 32,3200), right column (4448,0 to 32,3200).

    **7. Spawn Points (in Spawn Points layer)**
    - Place a Point object (not rectangle) inside each agent's home sector
    - Name = agent lowercase name: alice, bob, carla, david, emma, frank, grace, henry
    - Point must be on a walkable tile (NOT inside a collision rectangle)

    **8. Export**
    - File > Export As > `frontend/public/assets/tilemap/town.tmj`
    - Tile Layer Format: CSV
    - Do NOT embed tilesets in export (tileset PNGs are separate files already in the directory)

    **9. Verify**
    - After export, run: `python scripts/sync_map.py --tmj frontend/public/assets/tilemap/town.tmj`
    - Then run: `python scripts/validate_map.py`
    - Fix any errors reported and re-export.

    Per D-06: object layers for metadata (not tile properties).
    Per D-07: 10 visual layers matching reference.
    Per D-08: 4 metadata object layers.
    Per D-09: spawn point objects named per agent.
    Per D-13: this guide is the deliverable for the human dependency.
  </action>
  <verify>
    <automated>test -f /Users/sainobekou/projects/agent-town/docs/tiled-authoring-guide.md && wc -l /Users/sainobekou/projects/agent-town/docs/tiled-authoring-guide.md | awk '{print ($1 >= 50) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/tiled-authoring-guide.md` exits 0
    - `wc -l docs/tiled-authoring-guide.md` shows at least 50 lines
    - `grep -c "Bottom Ground\|Exterior Ground\|Wall\|Foreground L1\|Foreground L2\|Interior Ground" docs/tiled-authoring-guide.md` returns at least 6 (all visual layer names present)
    - `grep -c "Sectors\|Arenas\|Collision\|Spawn Points" docs/tiled-authoring-guide.md` returns at least 4 (all metadata layers mentioned)
    - `grep "sector:arena" docs/tiled-authoring-guide.md` confirms colon separator convention documented
    - `grep "sync_map.py" docs/tiled-authoring-guide.md` confirms verification step references the sync script
    - `grep "validate_map.py" docs/tiled-authoring-guide.md` confirms validation step is documented
    - `grep "visible.*false\|visible: false" docs/tiled-authoring-guide.md` confirms metadata layer visibility instruction
  </acceptance_criteria>
  <done>
    A complete Tiled authoring guide exists at docs/tiled-authoring-guide.md with all 9 sections. The user can follow it step-by-step to create the Agent Town map in Tiled with correct layer names, object conventions, custom properties, and export settings. All 14 sector names, all arena names from agent spatial trees, all 8 spawn point names, and all 16 tileset PNGs are listed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TMJ file -> sync_map.py | Local file parsed by sync script; user-authored Tiled export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | sync_map.py JSON parsing | mitigate | Wrap json.loads() in try/except with clear error message; validate required keys exist before processing |
| T-11-02 | Tampering | sync_map.py layer type confusion | mitigate | Validate metadata layers have type "objectgroup"; raise ValueError if tilelayer (Pitfall 4) |
| T-11-03 | Information Disclosure | sync_map.py path handling | accept | Script only reads/writes within project directory; no network access; local-only tool |
</threat_model>

<verification>
- `python -m pytest tests/test_sync_map.py -x -q` exits 0
- `scripts/sync_map.py` contains extract_map, extract_buildings, extract_spawn_points functions
- `docs/tiled-authoring-guide.md` contains all layer names and conventions
</verification>

<success_criteria>
1. sync_map.py correctly converts a minimal 10x10 TMJ fixture into valid town.json, buildings.json, and spawn_points.json with correct schemas
2. All unit tests pass (10+ test cases covering schema fidelity, collision priority, address assignment, error handling)
3. Tiled authoring guide is comprehensive enough for the user to create the map without asking clarifying questions
</success_criteria>

<output>
After completion, create `.planning/phases/11-town-map-design-backend-sync/11-01-SUMMARY.md`
</output>
