---
phase: 05-frontend
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/components/AgentSprite.tsx
  - frontend/src/components/MapCanvas.tsx
autonomous: true
requirements: [MAP-02, DSP-02]
must_haves:
  truths:
    - "Agent sprites appear at correct tile positions on the map as colored circles with initial letters"
    - "Agents smoothly interpolate between tile positions using lerp each frame"
    - "Each agent displays a name label below and current activity text above their sprite"
    - "The map supports click-drag panning and mouse wheel zooming"
    - "Clicking an agent circle sets selectedAgentId in the store"
    - "Click on empty map area does not trigger agent selection"
  artifacts:
    - path: "frontend/src/components/AgentSprite.tsx"
      provides: "PixiJS container with colored circle, initial letter, name label, activity label, lerp animation, click handler"
      contains: "useTick"
    - path: "frontend/src/components/MapCanvas.tsx"
      provides: "Viewport container with pan/zoom and agent sprite rendering"
      contains: "AgentSprite"
  key_links:
    - from: "frontend/src/components/AgentSprite.tsx"
      to: "frontend/src/store/simulationStore.ts"
      via: "useSimulationStore.getState() in useTick for position reads"
      pattern: "getState"
    - from: "frontend/src/components/MapCanvas.tsx"
      to: "frontend/src/store/simulationStore.ts"
      via: "useSimulationStore for agent IDs list and selectedAgentId"
      pattern: "useSimulationStore"
---

<objective>
Render agent sprites on the tile map with smooth movement, labels, pan/zoom navigation, and click-to-select interaction.

Purpose: MAP-02 requires agent sprites visible on the map moving in real-time. DSP-02 requires name and activity labels above each sprite. D-03 requires pan/zoom. D-11 requires click-to-select. This plan brings the map to life with agents that smoothly slide between tiles and can be inspected.

Output: Colored circle sprites with initial letters, name/activity labels, lerp movement, pan/zoom viewport, click-to-select wired to store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/MapCanvas.tsx
@frontend/src/store/simulationStore.ts
@frontend/src/types/index.ts
@backend/simulation/engine.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/types/index.ts (after Plan 01):
```typescript
export const TILE_SIZE = 32;

export interface AgentState {
  id: string;
  name: string;
  position: { x: number; y: number };  // pixel coords (tile * TILE_SIZE)
  activity: string;
  personality: string[];
  occupation: string;
  innate: string;
  age: number;
  currentLocation: string;
}

export interface SimulationStore {
  agents: Record<string, AgentState>;
  selectedAgentId: string | null;
  setSelectedAgent: (id: string | null) => void;
  // ... other actions from Plan 01
}
```

From @pixi/react v8 (verified in node_modules):
```typescript
import { Application, extend, useTick, useApplication } from "@pixi/react";
import { Container, Graphics, Text, TextStyle } from "pixi.js";
extend({ Container, Graphics, Text });

// useTick callback receives Ticker object
useTick((ticker) => { /* runs every animation frame */ });

// JSX elements: <pixiContainer>, <pixiGraphics>, <pixiText>
// Props: x, y, scale, interactive, onpointertap, ref
```

From backend/simulation/engine.py:
```python
TICK_INTERVAL = 5  # seconds between agent position updates
# Agent moves one tile per tick along BFS path
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentSprite component with lerp, labels, and click handler</name>
  <files>frontend/src/components/AgentSprite.tsx</files>
  <read_first>
    - frontend/src/types/index.ts (AgentState interface, TILE_SIZE constant — from Plan 01)
    - frontend/src/store/simulationStore.ts (agent state access pattern — from Plan 01)
    - frontend/src/components/MapCanvas.tsx (current PixiJS extend setup, v8 API patterns)
  </read_first>
  <action>
    Create frontend/src/components/AgentSprite.tsx:

    1. Define AGENT_COLORS array (8 distinct pastel/soft colors per D-12, one per agent):
       - 0xe74c3c (coral red), 0x3498db (sky blue), 0x2ecc71 (mint green), 0xf39c12 (amber),
         0x9b59b6 (lavender), 0x1abc9c (teal), 0xe67e22 (peach), 0x34495e (slate)
       - Assign by index: agent index in the agents object determines color

    2. Props: { agentId: string; colorIndex: number; onSelect: (id: string) => void }

    3. Create a pixiContainer (interactive=true, cursor="pointer") with:
       - A pixiGraphics drawing a filled circle (radius 12px) in the agent's assigned color
         - Use draw callback: g.clear(); g.setFillStyle({ color }); g.circle(0, 0, 12); g.fill()
         - Wrap in useCallback with [color] deps (color never changes per agent — stable reference)
       - A pixiText for the agent's first initial letter (white, bold, centered on the circle)
         - Position: x=-4 (approximate center), y=-6 (approximate center for 12px font)
         - Style: fontFamily "system-ui", fontSize 12, fontWeight "bold", fill 0xffffff
       - A pixiText for the agent's name BELOW the circle (per D-06)
         - Position: y=16 (below the circle)
         - Style: fontFamily "system-ui", fontSize 10, fill 0x333333
         - Anchor/position: center horizontally below circle
       - A pixiText for the current activity ABOVE the circle (per D-06)
         - Position: y=-28 (above the circle)
         - Style: fontFamily "system-ui", fontSize 9, fill 0x555555
         - Truncate activity text to 30 chars max to avoid overflow

    4. Lerp animation (per D-05):
       - Store currentPos in a useRef<{ x: number; y: number }>
       - Initialize currentPos to the agent's initial position from the store
       - In useTick callback:
         - Read target position from useSimulationStore.getState().agents[agentId]?.position
         - Read current activity from same source for updating the activity label
         - Compute lerp: currentPos.x += (target.x - currentPos.x) * 0.08
         - Compute lerp: currentPos.y += (target.y - currentPos.y) * 0.08
         - Update the container ref: containerRef.current.x = currentPos.x; containerRef.current.y = currentPos.y
         - Update activity text ref: activityTextRef.current.text = truncate(activity)
         - IMPORTANT: Use getState() (not the hook) inside useTick to avoid React re-renders per frame (Pitfall 2 and anti-pattern from research)
       - Lerp coefficient 0.08 at 60fps converges ~95% within 1.5 seconds (good for 5-second tick interval)

    5. Click handler:
       - On pixiContainer's onpointertap event: call onSelect(agentId)
       - The parent (MapCanvas) passes the handler that calls store.setSelectedAgent

    Per D-04: Colored circles with first initial letter inside.
    Per D-05: Smooth lerp interpolation between tile positions.
    Per D-06: Name label and current activity text displayed above/below each agent sprite at all times.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useTick" frontend/src/components/AgentSprite.tsx (lerp animation via PixiJS ticker)
    - grep -q "circle" frontend/src/components/AgentSprite.tsx (draws circle for agent sprite)
    - grep -q "getState" frontend/src/components/AgentSprite.tsx (reads from store without re-render)
    - grep -q "onpointertap\|pointertap" frontend/src/components/AgentSprite.tsx (click handler for selection)
    - grep -q "activity" frontend/src/components/AgentSprite.tsx (activity label rendered)
    - TypeScript compiles: cd frontend && npx tsc --noEmit
  </acceptance_criteria>
  <done>AgentSprite renders a colored circle with initial letter, name below, activity above, smoothly lerps between positions, and fires onSelect on click. All per D-04, D-05, D-06, D-11.</done>
</task>

<task type="auto">
  <name>Task 2: Pan/zoom viewport and agent rendering in MapCanvas</name>
  <files>frontend/src/components/MapCanvas.tsx</files>
  <read_first>
    - frontend/src/components/MapCanvas.tsx (current state after Plan 02 — has TileMap)
    - frontend/src/components/AgentSprite.tsx (just created in Task 1)
    - frontend/src/store/simulationStore.ts (agents record and setSelectedAgent)
    - frontend/src/types/index.ts (AgentState interface)
  </read_first>
  <action>
    Update frontend/src/components/MapCanvas.tsx to add pan/zoom viewport and render agent sprites:

    1. Pan/Zoom state (per D-03):
       - Add local state or refs for viewport: panX, panY, zoom (scale)
       - Initial zoom: 1.0. Min: 0.3, Max: 2.0 (Claude's discretion)
       - Initial pan: center the town in the viewport. Calculate initial offset based on container size.

    2. Pan implementation (click-drag):
       - Wrap the entire PixiJS Application in a div with onMouseDown, onMouseMove, onMouseUp, onMouseLeave handlers
       - Track drag state in a ref: { active: boolean, startX: number, startY: number, startPanX: number, startPanY: number }
       - Track hasDragged in a ref (boolean) — if the pointer moved more than 5px during the drag, it's a drag not a click (Pitfall 4 from research: prevents pan from triggering agent selection)
       - onMouseDown: set drag active, record starting positions
       - onMouseMove: if active, update panX/panY by delta from start
       - onMouseUp: set drag inactive
       - onMouseLeave: set drag inactive (prevent stuck drag)

    3. Zoom implementation (mouse wheel):
       - Add onWheel handler to the wrapper div
       - Compute new zoom: currentZoom * (1 - event.deltaY * 0.001)
       - Clamp between MIN_ZOOM (0.3) and MAX_ZOOM (2.0)
       - Zoom toward cursor position: adjust panX/panY to keep the point under the cursor stationary
       - Formula: newPan = cursorOffset - (cursorOffset - oldPan) * (newZoom / oldZoom)

    4. Viewport container:
       - The pixiContainer (viewport) wrapping TileMap and agents gets: x={panX}, y={panY}, scale={zoom}
       - Pass viewport ref for programmatic access if needed later

    5. Agent rendering:
       - Read agent IDs from Zustand store: const agentIds = useSimulationStore(s => Object.keys(s.agents))
       - For each agentId, render <AgentSprite key={agentId} agentId={agentId} colorIndex={index} onSelect={handleAgentClick} />
       - handleAgentClick: only fire if hasDragged is false (prevent drag triggering selection)
       - handleAgentClick calls store.setSelectedAgent(id)

    6. Click-on-empty-area to deselect (optional UX):
       - On viewport onpointertap (if not handled by an agent sprite): call setSelectedAgent(null)
       - Only fire if hasDragged is false

    Per D-03: Click-drag to pan the map, mouse wheel to zoom in/out. Camera centers on the town initially.
    Per D-11: Click an agent circle on the map to open their inspector.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AgentSprite" frontend/src/components/MapCanvas.tsx (renders agent sprites)
    - grep -q "onWheel\|wheel" frontend/src/components/MapCanvas.tsx (zoom handler present)
    - grep -q "onMouseDown\|mousedown\|pointerdown" frontend/src/components/MapCanvas.tsx (pan drag start handler)
    - grep -q "hasDragged\|has_dragged" frontend/src/components/MapCanvas.tsx (drag vs click disambiguation)
    - grep -q "setSelectedAgent" frontend/src/components/MapCanvas.tsx (wires click to store)
    - grep -q "scale" frontend/src/components/MapCanvas.tsx (zoom applied to viewport)
    - TypeScript compiles: cd frontend && npx tsc --noEmit
  </acceptance_criteria>
  <done>Map supports click-drag panning and mouse wheel zooming. Agent sprites render at correct positions from the store. Clicking an agent selects it (sets selectedAgentId). Dragging does not trigger selection. Camera starts centered on the town. All per D-03, D-04, D-11, MAP-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store -> PixiJS renderer | Agent positions from WebSocket feed into canvas rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | AgentSprite.tsx text rendering | accept | PixiJS Text renders to WebGL canvas — not DOM innerHTML; no XSS vector; agent names come from backend JSON configs |
| T-05-08 | Denial of Service | useTick lerp with many agents | accept | 8 agents max in v1; PixiJS ticker handles 25+ sprites without performance issues at 60fps |
</threat_model>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit`
2. Frontend dev server starts: `cd frontend && npm run dev`
3. All existing tests pass: `cd frontend && npm run test`
4. Visual: agents visible as colored circles on the map, smooth movement, pan/zoom works
</verification>

<success_criteria>
- Colored circle sprites with initial letters render at correct tile positions
- Name labels visible below each agent, activity text above
- Agents smoothly slide between positions when the simulation updates
- Click-drag pans the map, mouse wheel zooms in/out
- Clicking an agent sets selectedAgentId in the store
- Dragging does not accidentally select an agent
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-03-SUMMARY.md`
</output>
