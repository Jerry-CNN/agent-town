---
phase: 05-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routers/agents.py
  - backend/main.py
  - frontend/src/components/MapCanvas.tsx
  - frontend/src/components/TileMap.tsx
  - tests/test_agents_router.py
autonomous: true
requirements: [MAP-01]
must_haves:
  truths:
    - "The browser displays a scrollable 2D top-down tile map with all thematic town locations visually distinguished by color"
    - "Each sector zone has its name rendered as a text label centered on the zone"
    - "The map renders 3200x3200px canvas with sector bounding boxes computed from town.json"
    - "GET /api/agents/{agent_name}/memories returns last N memories sorted by created_at descending"
  artifacts:
    - path: "frontend/src/components/TileMap.tsx"
      provides: "PixiJS Graphics zones for all sectors with text labels"
      contains: "setFillStyle"
    - path: "frontend/src/components/MapCanvas.tsx"
      provides: "PixiJS Application with TileMap child and viewport container"
      contains: "Application"
    - path: "backend/routers/agents.py"
      provides: "GET /api/agents/{agent_name}/memories endpoint"
      contains: "get_agent_memories"
    - path: "tests/test_agents_router.py"
      provides: "Backend test for memories endpoint"
  key_links:
    - from: "frontend/src/components/TileMap.tsx"
      to: "backend/data/map/town.json"
      via: "fetch /api/map at runtime or import static JSON"
      pattern: "town.json"
    - from: "backend/routers/agents.py"
      to: "backend/agents/memory/store.py"
      via: "get_collection(simulation_id).get()"
      pattern: "get_collection"
---

<objective>
Render the tile map with colored sector zones and text labels, and create the backend memories API endpoint for the inspector panel.

Purpose: MAP-01 requires a 2D tile-map rendered in the browser with visually distinguished locations. This plan parses town.json sector data, computes bounding boxes, and draws colored rectangles with labels for each zone (park, cafe, shop, office, stock-exchange, wedding-hall, homes). Also creates the backend REST endpoint needed by Plan 04's inspector panel.

Output: Working tile map with 16 colored sectors visible in the browser. Backend /api/agents/{name}/memories endpoint returning sorted memories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/MapCanvas.tsx
@backend/data/map/town.json
@backend/agents/memory/store.py
@backend/routers/health.py
@backend/main.py
@backend/schemas.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/data/map/town.json (structure):
```json
{
  "world": "agent-town",
  "tile_size": 32,
  "size": [100, 100],
  "tile_address_keys": ["world", "sector", "arena"],
  "tiles": [
    {"coord": [x, y], "collision": true},
    {"coord": [x, y], "collision": false, "address": ["sector-name", "arena-name"]}
  ]
}
```
Sectors: cafe, home-alice, home-bob, home-carla, home-david, home-emma, home-frank, home-grace, home-henry, home-isabel, home-james, office, park, shop, stock-exchange, wedding-hall.
Total tiles: 4845 of 10000. Un-recorded tiles are implicit walkable ground (roads).

From backend/agents/memory/store.py:
```python
def get_collection(simulation_id: str) -> chromadb.Collection
# collection.get(where={"agent_id": name}, include=["documents", "metadatas"])
# Returns: {"ids": [...], "documents": [...], "metadatas": [...]}
# No ORDER BY — must sort by metadata.created_at after fetch
```

From backend/main.py:
```python
app.state.engine = engine  # SimulationEngine with .simulation_id attribute
app.include_router(health.router)
app.include_router(ws.router)
app.include_router(llm.router)
```

From MapCanvas.tsx (PixiJS v8 API):
```typescript
import { Application, extend } from "@pixi/react";
import { Container, Graphics, Text } from "pixi.js";
extend({ Container, Graphics, Text });
// v8 API: g.setFillStyle({ color: 0xRRGGBB }); g.rect(...); g.fill();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend memories endpoint</name>
  <files>backend/routers/agents.py, backend/main.py, tests/test_agents_router.py</files>
  <read_first>
    - backend/routers/health.py (existing router pattern for FastAPI router structure)
    - backend/agents/memory/store.py (get_collection, add_memory APIs)
    - backend/main.py (app.state.engine pattern, router registration)
    - backend/schemas.py (Memory model for reference)
    - backend/simulation/engine.py (engine.simulation_id attribute)
  </read_first>
  <action>
    1. Create backend/routers/agents.py:
       - Import APIRouter, Request from FastAPI
       - Import get_collection from backend.agents.memory.store
       - Create router = APIRouter(prefix="/api")
       - Add GET /api/agents/{agent_name}/memories endpoint:
         - Accept query param limit: int = 5
         - Access engine from request.app.state.engine
         - If engine is None, return 503 with {"detail": "Simulation not initialized"}
         - Call get_collection(engine.simulation_id).get(where={"agent_id": agent_name}, include=["documents", "metadatas"])
         - Wrap the ChromaDB .get() call with asyncio.to_thread() to avoid blocking the event loop (matches store.py pattern — Pitfall 1)
         - Sort results by metadata "created_at" descending
         - Slice to limit
         - Return {"memories": [{"content": doc, "type": meta["memory_type"], "importance": meta["importance"], "created_at": meta["created_at"]} for each]}
         - If no memories found, return {"memories": []}

    2. Register router in backend/main.py:
       - Import agents router: from backend.routers import agents
       - Add: app.include_router(agents.router) after existing router registrations

    3. Create tests/test_agents_router.py:
       - Test GET /api/agents/unknown/memories returns {"memories": []} (empty result, not 404)
       - Test that endpoint is registered and returns 200 or 503 (depending on engine state)
       - Use httpx.AsyncClient with app for testing (match existing test patterns if any)

    Security: agent_name comes from URL path — ChromaDB where filter uses it as-is. This is safe because ChromaDB metadata filters are string equality checks, not SQL (no injection risk). Single-user localhost application per threat model.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town && python -m pytest tests/test_agents_router.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "get_agent_memories" backend/routers/agents.py (endpoint function exists)
    - grep -q "agent_name" backend/routers/agents.py (path parameter used)
    - grep -q "get_collection" backend/routers/agents.py (uses ChromaDB store)
    - grep -q "agents.router" backend/main.py (router registered)
    - grep -q "created_at" backend/routers/agents.py (sorts by created_at)
    - pytest test passes for the endpoint
  </acceptance_criteria>
  <done>GET /api/agents/{agent_name}/memories returns last N memories sorted by created_at desc. Router registered in main.py. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: PixiJS tile map rendering with sector zones and labels</name>
  <files>frontend/src/components/TileMap.tsx, frontend/src/components/MapCanvas.tsx</files>
  <read_first>
    - frontend/src/components/MapCanvas.tsx (existing placeholder — will be replaced)
    - backend/data/map/town.json (tile structure, sector addresses, grid dimensions)
  </read_first>
  <action>
    1. Create frontend/src/components/TileMap.tsx:
       - Define TILE_SIZE = 32 (import from types/index.ts if available, otherwise local constant)
       - Define MAP_SIZE = 100 (tiles per axis)
       - Define sector color palette (soft/pastel per D-12):
         - park: 0xa8d5a2 (sage green)
         - cafe: 0xd4a96a (warm tan)
         - shop: 0xc9b99a (muted brown)
         - office: 0x9bb5c8 (slate blue)
         - stock-exchange: 0xc8b4e0 (soft purple)
         - wedding-hall: 0xf0c8d4 (blush pink)
         - home-*: 0xe8d5b7 (cream) for all 10 homes
         - collision (walls): 0x888880 (dark gray)
       - Define road/background color: 0xd0d0c8 (warm gray)
       - Parse town.json data to compute sector bounding boxes:
         - Group tiles by address[0] (sector name)
         - For each sector: compute minX, minY, maxX, maxY from tile coords
         - Store as array: { sector: string, x: number, y: number, width: number, height: number, color: number }
       - For the road background: draw a single full-canvas rectangle (MAP_SIZE * TILE_SIZE) in the road color FIRST
       - For collision tiles (tiles with collision=true and no address): draw individual rectangles (they are sparse — border walls)
       - For each sector zone: draw a filled rectangle using setFillStyle + rect + fill (PixiJS v8 API)
       - For each sector zone: add a pixiText label centered in the zone with the sector name (formatted: replace hyphens with spaces, title case)
       - Use useCallback with EMPTY deps array for the draw function (static map data — no dynamic deps, prevents re-draw per frame, per Pitfall 3 from research)
       - Import town.json statically: import townData from "../../backend/data/map/town.json" (Vite supports JSON imports) OR fetch from a served endpoint. Prefer static import for simplicity since the map is fixed.

       IMPORTANT: If static JSON import from backend directory doesn't work due to Vite's root config, create a copy at frontend/public/town.json or use a fetch. The executor should check Vite's config first.

    2. Replace frontend/src/components/MapCanvas.tsx:
       - Keep the existing extend({ Container, Graphics, Text }) call
       - Replace the grass placeholder content with:
         - An Application component (keep resizeTo behavior but DO NOT use resizeTo={window} — instead, let the parent div control size)
         - A single pixiContainer as the viewport (this will be the pan/zoom target in Plan 03 — for now, just render at origin)
         - TileMap component as a child inside the viewport container
       - The Application background color should be 0xd0d0c8 (warm gray, matches road color) per D-12

    Per D-01: Colored rectangles with text labels for tiles. Green for park, brown for buildings, gray for roads.
    Per D-02: 32px tile size, 100x100 grid = 3200x3200px canvas.
    Per D-12: Clean and minimal with soft/pastel colors.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "setFillStyle" frontend/src/components/TileMap.tsx (uses PixiJS v8 fill API)
    - grep -q "0xa8d5a2" frontend/src/components/TileMap.tsx (park color present)
    - grep -q "0xc8b4e0" frontend/src/components/TileMap.tsx (stock-exchange color present)
    - grep -q "pixiText\|Text" frontend/src/components/TileMap.tsx (sector label text rendered)
    - grep -q "TileMap" frontend/src/components/MapCanvas.tsx (TileMap imported and rendered)
    - grep -q "Application" frontend/src/components/MapCanvas.tsx (PixiJS Application still present)
    - TypeScript compiles: cd frontend && npx tsc --noEmit
  </acceptance_criteria>
  <done>The browser displays a 2D top-down tile map with 16 colored sector zones (park green, cafe tan, office blue, etc.), text labels centered on each zone, dark gray collision tiles around the border, and warm gray road background. MAP-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/agents/{name}/memories | Agent name from browser used in ChromaDB query |
| town.json -> browser | Static map data loaded into PixiJS renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | agents.py agent_name path param | accept | ChromaDB where filter uses string equality — no SQL injection possible; single-user localhost app |
| T-05-05 | Information Disclosure | memories endpoint | accept | Returns agent memories to localhost browser; no PII, single-user architecture, no auth needed for v1 |
| T-05-06 | Denial of Service | memories endpoint limit param | mitigate | Clamp limit to max 50 in endpoint to prevent unbounded ChromaDB fetch |
</threat_model>

<verification>
1. Backend tests pass: `cd /Users/sainobekou/projects/agent-town && python -m pytest tests/test_agents_router.py -v`
2. Frontend TypeScript compiles: `cd frontend && npx tsc --noEmit`
3. Frontend dev server starts without errors: `cd frontend && npm run dev` (visual check)
4. All existing tests pass: `cd frontend && npm run test`
</verification>

<success_criteria>
- Tile map renders in the browser with colored zones for all 16 sectors
- Each zone has a readable text label
- Road areas are warm gray background
- Collision/wall tiles are dark gray
- GET /api/agents/{agent_name}/memories returns sorted memories with correct schema
- TypeScript compiles without errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-02-SUMMARY.md`
</output>
