---
phase: 05-frontend
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/components/ActivityFeed.tsx
  - frontend/src/components/AgentInspector.tsx
  - frontend/src/components/Layout.tsx
  - frontend/src/tests/activityFeed.test.tsx
  - frontend/src/tests/inspector.test.tsx
autonomous: true
requirements: [MAP-05, DSP-01]
must_haves:
  truths:
    - "Activity feed shows formatted entries with colored agent name, action description, and timestamp"
    - "Activity feed auto-scrolls to bottom on new entries but pauses when user scrolls up"
    - "Clicking an agent opens the inspector panel replacing the activity feed in the sidebar"
    - "Inspector shows agent name, occupation, personality traits, current activity, location, and last 5 memory entries"
    - "Closing the inspector restores the activity feed"
    - "Inspector fetches memories from GET /api/agents/{name}/memories"
  artifacts:
    - path: "frontend/src/components/ActivityFeed.tsx"
      provides: "Formatted feed entries with agent name coloring, scroll-pause behavior"
      contains: "scrollIntoView"
    - path: "frontend/src/components/AgentInspector.tsx"
      provides: "Agent profile panel with personality, activity, location, memories"
      contains: "memories"
    - path: "frontend/src/components/Layout.tsx"
      provides: "Sidebar switching between ActivityFeed and AgentInspector"
      contains: "AgentInspector"
    - path: "frontend/src/tests/activityFeed.test.tsx"
      provides: "Tests for feed entry format and rendering"
    - path: "frontend/src/tests/inspector.test.tsx"
      provides: "Tests for inspector rendering with mocked agent data"
  key_links:
    - from: "frontend/src/components/AgentInspector.tsx"
      to: "backend/routers/agents.py"
      via: "fetch /api/agents/{name}/memories"
      pattern: "fetch.*memories"
    - from: "frontend/src/components/Layout.tsx"
      to: "frontend/src/components/AgentInspector.tsx"
      via: "selectedAgentId conditional rendering"
      pattern: "selectedAgentId"
---

<objective>
Upgrade the activity feed to show formatted entries and build the agent inspector panel for agent inspection.

Purpose: DSP-01 requires a real-time activity feed with colored agent names and timestamps. MAP-05 requires clicking an agent to inspect their personality, activity, and memories. The inspector replaces the feed in the sidebar (D-10) and fetches memories from the backend endpoint created in Plan 02.

Output: Formatted activity feed with scroll-pause behavior, agent inspector panel with personality/memories display, sidebar switching logic in Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/ActivityFeed.tsx
@frontend/src/components/Layout.tsx
@frontend/src/store/simulationStore.ts
@frontend/src/types/index.ts
@backend/routers/agents.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/types/index.ts (after Plan 01):
```typescript
export interface AgentState {
  id: string;
  name: string;
  position: { x: number; y: number };
  activity: string;
  personality: string[];
  occupation: string;
  innate: string;
  age: number;
  currentLocation: string;
}

export interface WSMessage {
  type: WSMessageType;
  payload: Record<string, unknown>;
  timestamp: number;
}
```

From frontend/src/store/simulationStore.ts (after Plan 01):
```typescript
// Store shape:
agents: Record<string, AgentState>;
selectedAgentId: string | null;
setSelectedAgent: (id: string | null) => void;
feed: WSMessage[];  // only conversation and event messages
```

From backend/routers/agents.py (after Plan 02):
```
GET /api/agents/{agent_name}/memories?limit=5
Response: {
  "memories": [
    {"content": str, "type": str, "importance": int, "created_at": float}
  ]
}
```

Agent colors for matching feed entry names (from Plan 03 AgentSprite):
```typescript
const AGENT_COLORS = [
  "#e74c3c", "#3498db", "#2ecc71", "#f39c12",
  "#9b59b6", "#1abc9c", "#e67e22", "#34495e"
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade ActivityFeed with formatted entries and scroll-pause</name>
  <files>frontend/src/components/ActivityFeed.tsx, frontend/src/tests/activityFeed.test.tsx</files>
  <read_first>
    - frontend/src/components/ActivityFeed.tsx (current raw JSON dump implementation)
    - frontend/src/store/simulationStore.ts (feed array shape — WSMessage[] after Plan 01)
    - frontend/src/types/index.ts (WSMessage and WSMessageType definitions)
    - frontend/src/tests/store.test.ts (test patterns for reference)
  </read_first>
  <behavior>
    - Test 1: A conversation WSMessage renders with agent name styled in color, action text, and formatted timestamp (HH:MM:SS)
    - Test 2: An event WSMessage renders with "Event" prefix and the event text
    - Test 3: Empty feed renders "No activity yet." placeholder text
    - Test 4: Feed container has overflowY auto for scrolling
  </behavior>
  <action>
    1. Create frontend/src/tests/activityFeed.test.tsx:
       - Import render, screen from @testing-library/react
       - Mock the Zustand store with test feed data
       - Test that conversation message renders formatted: agent name (colored span) + action text + timestamp
       - Test that event message renders with "Event:" prefix
       - Test empty state shows "No activity yet."

    2. Upgrade frontend/src/components/ActivityFeed.tsx:
       - Define AGENT_COLORS map matching Plan 03's color palette: Map agent names to hex color strings for consistent coloring between map sprites and feed entries. Use a simple hash function on agent name to pick from the array (same index logic as AgentSprite colorIndex):
         const AGENT_COLORS_HEX = ["#e74c3c", "#3498db", "#2ecc71", "#f39c12", "#9b59b6", "#1abc9c", "#e67e22", "#34495e"];
         function getAgentColor(name: string): string — hash the name to get consistent index

       - Format each feed entry based on msg.type:
         - "conversation": Extract turns from payload. For each turn, show: "[HH:MM:SS] AgentName: text"
           AgentName should be colored span using the agent's assigned color
         - "event": Show: "[HH:MM:SS] Event: {payload.text or payload description}"
         - Timestamp: convert msg.timestamp (Unix seconds) to HH:MM:SS format

       - Auto-scroll with user-override (per D-07):
         - Add containerRef for the scrollable div
         - Add userScrolled ref (boolean)
         - Add onScroll handler to the container:
           const el = containerRef.current;
           const atBottom = el.scrollHeight - el.scrollTop - el.clientHeight < 10;
           userScrolled.current = !atBottom;
         - In useEffect on [feed]: only call scrollIntoView if !userScrolled.current
         - When user scrolls back to bottom, auto-scroll resumes

       - Style per D-12: dark sidebar background (#111827 — already set by Layout), light text, clean sans-serif
       - Each entry: subtle background (rgba(255,255,255,0.04)), small padding, rounded corners (existing style preserved)

    Per D-07: Auto-scrolling log with latest entries at the bottom. Auto-scroll pauses when user scrolls up; resumes when scrolled to bottom.
    Per D-08: Each feed entry shows agent name (colored to match their circle), action description, and timestamp.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npm run test -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "userScrolled\|userScroll" frontend/src/components/ActivityFeed.tsx (scroll-pause behavior)
    - grep -q "onScroll\|handleScroll" frontend/src/components/ActivityFeed.tsx (scroll event handler)
    - grep -q "HH\|toLocaleTimeString\|getHours\|padStart" frontend/src/components/ActivityFeed.tsx (timestamp formatting)
    - grep -q "color" frontend/src/components/ActivityFeed.tsx (agent name coloring)
    - grep -q "conversation" frontend/src/components/ActivityFeed.tsx (handles conversation type)
    - grep -q "activityFeed\|ActivityFeed" frontend/src/tests/activityFeed.test.tsx (test file exists with tests)
    - All tests pass: cd frontend && npm run test
  </acceptance_criteria>
  <done>Activity feed shows formatted entries with colored agent names, action descriptions, and HH:MM:SS timestamps. Auto-scroll pauses when user scrolls up and resumes when scrolled to bottom. DSP-01 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Agent inspector panel with personality and memories</name>
  <files>frontend/src/components/AgentInspector.tsx, frontend/src/components/Layout.tsx, frontend/src/tests/inspector.test.tsx</files>
  <read_first>
    - frontend/src/components/Layout.tsx (current sidebar switching — placeholder for inspector)
    - frontend/src/store/simulationStore.ts (agents record, selectedAgentId, setSelectedAgent)
    - frontend/src/types/index.ts (AgentState with personality fields from Plan 01)
    - backend/routers/agents.py (memories endpoint response format from Plan 02)
  </read_first>
  <action>
    1. Create frontend/src/components/AgentInspector.tsx:
       - Props: { agentId: string; onClose: () => void }
       - Read agent data from Zustand store: useSimulationStore(s => s.agents[agentId])
       - If agent not found in store, show "Agent not found" message

       - Display sections (per D-09):
         a. Header: Agent name (large, bold) + close button (top-right "X")
         b. Profile section:
            - Occupation: agent.occupation or "Unknown"
            - Age: agent.age or "—"
            - Personality: agent.innate (displayed as comma-separated trait tags with subtle background)
         c. Current state section:
            - Activity: agent.activity
            - Location: agent.currentLocation or "Unknown"
         d. Recent memories section:
            - Title: "Recent Memories"
            - Fetch memories on mount and when agentId changes:
              useEffect(() => {
                fetch(`/api/agents/${encodeURIComponent(agentId)}/memories?limit=5`)
                  .then(r => r.json())
                  .then(data => setMemories(data.memories))
                  .catch(() => setMemories([]));
              }, [agentId]);
            - Render each memory: content text + importance badge (1-10) + formatted timestamp
            - If no memories: show "No memories yet."
            - Loading state: show "Loading memories..." while fetching

       - Style per D-12:
         - Dark sidebar background (inherited from Layout)
         - Clean sans-serif fonts (system-ui)
         - Subtle section dividers
         - Personality trait tags: small rounded pills with translucent background
         - Memory entries: subtle card-like appearance with left border colored by importance

    2. Update frontend/src/components/Layout.tsx:
       - Import AgentInspector component
       - Replace the inspector placeholder div with:
         <AgentInspector agentId={selectedAgentId!} onClose={() => setSelectedAgent(null)} />
       - Note: Layout already has the conditional: selectedAgentId !== null ? inspector : feed
         Just replace the placeholder with the real component
       - Import setSelectedAgent from store (or pass as prop — currently selectedAgentId comes as prop from App.tsx. Add the close handler)
       - The close handler calls useSimulationStore.getState().setSelectedAgent(null)

    3. Create frontend/src/tests/inspector.test.tsx:
       - Mock Zustand store with a test agent (name, occupation, innate, activity, etc.)
       - Mock fetch for /api/agents/.../memories to return test memories
       - Test that inspector renders agent name
       - Test that inspector renders occupation and traits
       - Test that inspector renders current activity
       - Test that close button calls onClose
       - Test that memories section renders fetched memories

    Per D-09: Full profile panel: name, occupation, personality traits (innate), current activity, current location, and last 5 memory entries.
    Per D-10: Inspector opens in the sidebar, replacing the activity feed. Closing inspector restores the feed.
    Per D-11: Close button or click another agent to switch.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npm run test -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "memories" frontend/src/components/AgentInspector.tsx (fetches and displays memories)
    - grep -q "fetch.*memories" frontend/src/components/AgentInspector.tsx (REST call to memories endpoint)
    - grep -q "occupation\|Occupation" frontend/src/components/AgentInspector.tsx (displays occupation)
    - grep -q "innate\|traits\|personality" frontend/src/components/AgentInspector.tsx (displays personality traits)
    - grep -q "onClose" frontend/src/components/AgentInspector.tsx (close handler prop)
    - grep -q "AgentInspector" frontend/src/components/Layout.tsx (inspector component imported and rendered)
    - grep -q "setSelectedAgent" frontend/src/components/Layout.tsx (close handler wired)
    - grep -q "AgentInspector\|inspector" frontend/src/tests/inspector.test.tsx (test file exists)
    - All tests pass: cd frontend && npm run test
  </acceptance_criteria>
  <done>Agent inspector shows full profile (name, occupation, traits, activity, location, last 5 memories). Opens in sidebar replacing the feed. Close button restores the feed. Memories fetched from backend on demand. MAP-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend /api/agents/*/memories -> browser | Agent memories rendered as text in React components |
| Zustand store agent data -> inspector DOM | Agent personality data rendered in DOM text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | AgentInspector.tsx memory rendering | accept | React renders text content via JSX text interpolation (auto-escaped, no innerHTML); memory content from backend ChromaDB is not user-supplied input in v1 |
| T-05-10 | Information Disclosure | Inspector personality display | accept | Single-user localhost app; personality data is from static agent configs, not private user data |
| T-05-11 | Spoofing | fetch to /api/agents/* | accept | Localhost-only; no CSRF risk for GET endpoints returning non-sensitive simulation data |
</threat_model>

<verification>
1. All frontend tests pass: `cd frontend && npm run test`
2. TypeScript compiles: `cd frontend && npx tsc --noEmit`
3. Activity feed shows formatted entries (not raw JSON)
4. Clicking an agent on the map opens the inspector in the sidebar
5. Inspector shows agent name, occupation, traits, activity, location, and memories
6. Closing the inspector restores the activity feed
7. Auto-scroll pauses when user scrolls up in the feed
</verification>

<success_criteria>
- Activity feed entries show colored agent name, action description, and timestamp
- Feed auto-scrolls but pauses when user scrolls up
- Inspector panel displays full agent profile with personality traits and memories
- Inspector replaces feed in sidebar; close restores feed
- Memories fetched from /api/agents/{name}/memories endpoint
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-04-SUMMARY.md`
</output>
