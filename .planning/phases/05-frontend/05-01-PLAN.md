---
phase: 05-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/store/simulationStore.ts
  - frontend/src/hooks/useWebSocket.ts
  - frontend/src/components/BottomBar.tsx
  - frontend/src/tests/dispatch.test.ts
autonomous: true
requirements: [MAP-02, DSP-02]
must_haves:
  truths:
    - "WebSocket snapshot message initializes all agent positions and activities in Zustand store"
    - "WebSocket agent_update message updates a single agent's position and activity in store"
    - "WebSocket simulation_status message updates isPaused in store"
    - "Only conversation and event messages appear in the feed array; snapshot and agent_update do not"
    - "BottomBar pause/resume button sends pause/resume WSMessage to backend via WebSocket"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "Extended WSMessageType union and AgentState with personality fields"
      contains: "snapshot"
    - path: "frontend/src/store/simulationStore.ts"
      provides: "updateAgentsFromSnapshot, updateAgentPosition, sendMessage actions"
      exports: ["useSimulationStore"]
    - path: "frontend/src/hooks/useWebSocket.ts"
      provides: "Message dispatch by type and sendMessage exposure"
      contains: "case \"snapshot\""
    - path: "frontend/src/tests/dispatch.test.ts"
      provides: "Store dispatch unit tests for snapshot, agent_update, simulation_status"
  key_links:
    - from: "frontend/src/hooks/useWebSocket.ts"
      to: "frontend/src/store/simulationStore.ts"
      via: "getState() dispatch by msg.type"
      pattern: "case .snapshot."
    - from: "frontend/src/components/BottomBar.tsx"
      to: "backend/routers/ws.py"
      via: "sendMessage({type: 'pause'|'resume'})"
      pattern: "sendMessage"
---

<objective>
Upgrade the frontend data layer to properly dispatch WebSocket messages by type and expose agent state in the Zustand store.

Purpose: The existing useWebSocket hook dumps ALL messages into appendFeed indiscriminately. This plan routes snapshot, agent_update, conversation, simulation_status, and event messages to their correct store actions, making agent positions and activities available for rendering in Plans 02-04. Also wires BottomBar pause/resume to actually send WebSocket commands to the backend.

Output: Working WebSocket dispatch, Zustand store with per-agent state, sendMessage for BottomBar, unit tests for dispatch logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/types/index.ts
@frontend/src/store/simulationStore.ts
@frontend/src/hooks/useWebSocket.ts
@frontend/src/components/BottomBar.tsx
@frontend/src/tests/store.test.ts
@backend/schemas.py
@backend/simulation/engine.py
@backend/routers/ws.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/schemas.py:
```python
class WSMessage(BaseModel):
    type: Literal[
        "agent_update", "conversation", "simulation_status", "snapshot",
        "event", "ping", "pong", "error", "pause", "resume",
    ]
    payload: dict
    timestamp: float
```

From backend/simulation/engine.py:
```python
# get_snapshot() returns:
{
    "agents": [
        {"name": str, "coord": [int, int], "activity": str}
    ],
    "simulation_status": "running" | "paused",
    "tick_count": int,
}

# _emit_agent_update broadcasts:
{"type": "agent_update", "name": str, "coord": [int, int], "activity": str}

# _emit_conversation broadcasts:
{"type": "conversation", "turns": [...], "summary": str}
```

From backend/routers/ws.py:
```python
# Inbound commands accepted: "pause", "resume", "ping"
# Snapshot sent on connect BEFORE delta registration
```

From backend/schemas.py AgentConfig:
```python
class AgentConfig(BaseModel):
    name: str
    coord: tuple[int, int]
    currently: str
    scratch: AgentScratch  # age, innate, learned, lifestyle, daily_plan

class AgentScratch(BaseModel):
    age: int
    innate: str
    learned: str
    lifestyle: str
    daily_plan: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend types and Zustand store with agent dispatch actions</name>
  <files>frontend/src/types/index.ts, frontend/src/store/simulationStore.ts, frontend/src/tests/dispatch.test.ts</files>
  <read_first>
    - frontend/src/types/index.ts (current type definitions)
    - frontend/src/store/simulationStore.ts (current store actions)
    - frontend/src/tests/store.test.ts (existing test patterns)
    - backend/schemas.py (WSMessage type Literal — source of truth for message types)
    - backend/simulation/engine.py (get_snapshot() return structure, _emit_agent_update payload)
  </read_first>
  <behavior>
    - Test 1: updateAgentsFromSnapshot with 3 agents sets store.agents to Record with 3 entries keyed by name, each with position = coord * 32
    - Test 2: updateAgentPosition("Alice Chen", [15, 42], "walking") updates store.agents["Alice Chen"].position to {x: 480, y: 1344} and activity to "walking"
    - Test 3: updateAgentPosition for non-existent agent creates a new entry (safe against race)
    - Test 4: setPaused(true) sets store.isPaused to true
    - Test 5: appendFeed only accepts conversation and event types (verify feed grows for these, stays empty when calling updateAgentsFromSnapshot)
    - Test 6: setSendMessage stores a function ref; getSendMessage returns it
  </behavior>
  <action>
    1. In types/index.ts:
       - Extend WSMessageType to: "agent_update" | "conversation" | "simulation_status" | "snapshot" | "event" | "ping" | "pong" | "error" | "pause" | "resume"
       - Add personality fields to AgentState: occupation (string), innate (string), age (number), currentLocation (string) — these will be populated from snapshot extension (Plan 02 backend task) or defaults
       - Add TILE_SIZE constant: export const TILE_SIZE = 32
       - Add SnapshotAgent interface: { name: string; coord: [number, number]; activity: string; occupation?: string; innate?: string; age?: number }
       - Add SimulationStore actions: updateAgentsFromSnapshot, updateAgentPosition, setSendMessage (stores a ref to WS send function)

    2. In store/simulationStore.ts:
       - Add updateAgentsFromSnapshot(agents: SnapshotAgent[]) — converts coord to pixel position (coord[0]*TILE_SIZE, coord[1]*TILE_SIZE), creates Record keyed by name
       - Add updateAgentPosition(name: string, coord: [number, number], activity: string) — updates single agent, converts coord to pixels. If agent doesn't exist, creates a minimal entry (safe for race conditions)
       - Add _sendMessage ref: stored as a non-reactive ref via a module-level variable (not in Zustand state — avoids re-renders). Expose setSendMessage and getSendMessage.
       - Keep existing appendFeed, setAgents, setConnected, setPaused, setSelectedAgent, setProviderConfig, reset

    3. In tests/dispatch.test.ts:
       - Write tests for all behaviors listed above
       - Use TILE_SIZE = 32 constant for coordinate assertions
       - Verify updateAgentsFromSnapshot replaces agents entirely (not merges)

    Per D-02: TILE_SIZE = 32 (32px tiles on 100x100 grid = 3200x3200px canvas).
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npm run test -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "snapshot" frontend/src/types/index.ts (WSMessageType includes snapshot)
    - grep -q "TILE_SIZE" frontend/src/types/index.ts (constant exported)
    - grep -q "updateAgentsFromSnapshot" frontend/src/store/simulationStore.ts (action exists)
    - grep -q "updateAgentPosition" frontend/src/store/simulationStore.ts (action exists)
    - grep -q "setSendMessage" frontend/src/store/simulationStore.ts (send function ref stored)
    - grep -q "updateAgentsFromSnapshot" frontend/src/tests/dispatch.test.ts (test exists)
    - All tests pass: cd frontend && npm run test
  </acceptance_criteria>
  <done>Types extended with full WSMessageType union and SnapshotAgent. Store has updateAgentsFromSnapshot, updateAgentPosition, and sendMessage ref. All dispatch tests pass. Existing store tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade useWebSocket dispatch and wire BottomBar pause/resume</name>
  <files>frontend/src/hooks/useWebSocket.ts, frontend/src/components/BottomBar.tsx</files>
  <read_first>
    - frontend/src/hooks/useWebSocket.ts (current appendFeed-for-all pattern)
    - frontend/src/components/BottomBar.tsx (current local-only setPaused)
    - frontend/src/store/simulationStore.ts (newly added actions from Task 1)
    - frontend/src/types/index.ts (updated WSMessageType from Task 1)
  </read_first>
  <action>
    1. In hooks/useWebSocket.ts:
       - Replace the ws.onmessage handler's blanket appendFeed call with a switch on msg.type:
         - "snapshot": call store.updateAgentsFromSnapshot(msg.payload.agents) and store.setPaused(msg.payload.simulation_status === "paused")
         - "agent_update": call store.updateAgentPosition(msg.payload.name, msg.payload.coord, msg.payload.activity)
         - "conversation": call store.appendFeed(msg) — these belong in the activity feed
         - "simulation_status": call store.setPaused(msg.payload.status === "paused")
         - "event": call store.appendFeed(msg) — user-injected events belong in the feed
         - "pong", "error": log to console, do not append to feed
         - "ping": ignore (server won't send pings to client)
       - On ws.onopen: after setConnected(true), store a sendMessage function:
         store.setSendMessage((msg) => ws.send(JSON.stringify(msg)))
       - On cleanup: store.setSendMessage(null)
       - Change return type to { isConnected: boolean } (unchanged externally)

    2. In components/BottomBar.tsx:
       - Import getSendMessage from the store module (or access via useSimulationStore)
       - Replace onClick handler: instead of just setPaused(!isPaused), call:
         const send = getSendMessage();
         if (send) {
           send({ type: isPaused ? "resume" : "pause", payload: {}, timestamp: Date.now() / 1000 });
         }
       - Do NOT call setPaused locally — the backend will broadcast simulation_status back, which the WS dispatch handler (above) will set isPaused. This ensures single source of truth.
       - Keep the disabled event input and provider badge as-is (Phase 6 wiring).

    Per D-08 (Phase 4): pause/resume commands sent via WebSocket from the browser.
  </action>
  <verify>
    <automated>cd /Users/sainobekou/projects/agent-town/frontend && npm run test -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'case "snapshot"' frontend/src/hooks/useWebSocket.ts (dispatch routes snapshot)
    - grep -q 'case "agent_update"' frontend/src/hooks/useWebSocket.ts (dispatch routes agent_update)
    - grep -q 'case "conversation"' frontend/src/hooks/useWebSocket.ts (dispatch routes conversation)
    - grep -q "setSendMessage" frontend/src/hooks/useWebSocket.ts (stores send function)
    - grep -q "getSendMessage\|sendMessage" frontend/src/components/BottomBar.tsx (uses send function)
    - grep -v "setPaused(!isPaused)" frontend/src/components/BottomBar.tsx confirms no local toggle (single source of truth)
    - All existing tests pass: cd frontend && npm run test
  </acceptance_criteria>
  <done>WebSocket messages dispatch to correct store actions by type. BottomBar sends pause/resume commands to backend via WebSocket. isPaused is driven by backend broadcast, not local state. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket server -> browser | Agent data from backend crosses into browser rendering context |
| Browser -> WebSocket server | Pause/resume commands sent from browser to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | useWebSocket.ts message parsing | mitigate | JSON.parse wrapped in try/catch (already present); malformed messages logged and discarded, never inserted into store |
| T-05-02 | Tampering | BottomBar.tsx outbound commands | accept | Only sends pause/resume type strings; backend validates via Pydantic WSMessage.model_validate_json — invalid types return error response (already implemented in ws.py T-04-04) |
| T-05-03 | Information Disclosure | Agent state in Zustand store | accept | Single-user local application; agent data is not sensitive; no PII |
</threat_model>

<verification>
1. Run full frontend test suite: `cd frontend && npm run test`
2. All new dispatch tests pass (updateAgentsFromSnapshot, updateAgentPosition, setPaused)
3. All existing store tests pass (no regression)
4. TypeScript compiles without errors: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- WebSocket messages are routed by type to the correct store actions
- Snapshot initializes all agents in the store with pixel-coordinate positions
- agent_update updates a single agent's position and activity
- conversation and event messages appear in the feed; snapshot and agent_update do not
- BottomBar pause/resume sends WebSocket commands to backend
- isPaused is driven by backend simulation_status, not local toggle
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-01-SUMMARY.md`
</output>
